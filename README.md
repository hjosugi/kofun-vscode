# Frost VS Code metadata

This is intentionally a syntax-only bootstrap extension. It provides `.frost` language registration, comments, brackets, indentation, and TextMate highlighting. A language server is part of the backlog and is not bundled here.

To test locally, open `editor/vscode` with VS Code's extension development host.
